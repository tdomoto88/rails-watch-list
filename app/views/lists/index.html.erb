<h1 class="my-4">All Lists</h1>
<%= link_to 'New List', new_list_path, class: 'btn btn-primary mb-4' %>
<ul class="list-group">
  <% @lists.each do |list| %>
    <li class="list-group-item"><%= link_to list.name, list_path(list) %></li>
  <% end %>
</ul>
